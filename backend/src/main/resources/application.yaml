spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres # jdbc:postgresql://158.160.59.159:5555/test
    username: postgres
    password: admin
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
server:
  port: 8080
jwt:
  secret:
    key: asdjkmasdmkaskdmaskmd

springdoc:
  swagger-ui:
    path: /docs
